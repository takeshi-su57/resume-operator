# Architecture

This file is kept for legacy link compatibility. The canonical architecture document — covering the LangGraph engine, the FastAPI server, the desktop GUI layer, the Prompter seam, the WebSocket protocol, and the sidecar lifecycle — lives at:

[**../architecture.md**](../architecture.md)

ADRs (Architecture Decision Records) live in [`adr/`](adr/) alongside this redirect.
